<div class="row">
  <div class="large-8 columns large-centered nav-border">
    <div id='dbear' class='hide'><img src='/assets/dbear.gif'></div>
    <h1 class="hero">PROJECT<span>3</span></h1>
    <nav class="top-bar">
      <section class="top-bar-section">

        <!-- Right Nav Section -->
        <ul>
          <% if @auth %>
            <li><%= link_to("Explore", careers_path ) %></li>
            <li><%= link_to("My Careers", mycareer_careers_path ) %></li>
            <li><%= link_to("My Jobs", jobs_path ) %></li>
            <li><%= link_to("My Dashboard", @auth ) %></li>
            <li><%= link_to("Logout", login_path, :method => 'delete') %></li>
          <% else %>
            <li><%= link_to("Create Account", new_user_path, :remote => true ) %></li>
            <li><%= link_to("Login", login_path, :remote => true ) %></li>
          <% end %>
        </ul>
      </section>
    </nav>
  </div>
</div>