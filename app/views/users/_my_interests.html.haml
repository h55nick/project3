.row
  - if !@auth.finished_all_questions
    .small-4.large-2.columns
      .survey-numeric
        %h3= link_to('Return', survey_path)
        %strong To Survey
  .large-8.columns{ class: (@auth.finished_all_questions ? 'small-8 push-2' : 'small-4' ) }
    .survey-progress.progress.large-12.success
      %span.meter{ style: "width: #{((@user.questions.length.to_f / Question.all.length) * 100).to_i}%;" }
  .small-4.large-2.columns
    .survey-numeric
      %h3= "#{((@user.questions.length.to_f / Question.all.length) * 100).to_i}%"
      %strong Completed
.row
  .large-12.columns
    .wide-color
      .row
        -  @user.get_top_interests[0..2].each do |interest|
          -des = Interest.definition(interest)
          .large-4.columns
            .sectional-block{ style: "background: #{@user.show_color(interest)}" }
              .sectional{ style: "background: url('/assets/#{interest}-icon.svg') no-repeat center;" }
            %h5!= "<b>#{interest}</b> <small>/ #{des[:adj]}</small>"
            %p="#{des[:descrip]}"

.row
  .large-12.columns
    %hr
    %h1 What now?
.row
  .large-6.small-12.columns
    %h3 Explore Careers
    %h5 Go to the Explore page to search for all the careers that match your interests and personality. You can always return to the survey to get more accurate results.
  .large-6.small-12.columns
    %h3 Manage Your Job Search
    %h5 Use the Project3 Bookmarklet on the final tab to save jobs from outside sites including Idealist.org, Indeed.com, and AuthenticJobs.com.

