.row
  .large-6.columns
    %h4
      Welcome to CareerCatch.
    %p!="Please " + link_to("Login", login_path, :remote => true ) +" or " +link_to("Create an Account", new_user_path, :remote => true ) +" ."
    %p
    You are just a few steps away from finding the perfect career and the perfect job!
  .large-6.columns
    -if current_user.nil?
      #user_form
        =render :partial => 'session/form'
