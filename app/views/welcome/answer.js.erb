<% if @auth.questions.length != Question.all.length %>
    parallax.q<%= @next_question %>.right();
    $('#show-survey-progress').empty().append('<%=j render partial: 'survey_progress' %>');
<% else %>
  document.location.href = '/';
<% end %>